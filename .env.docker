# ─── Docker Infrastructure ─────────────────────────────────────────────
# Copy this file to .env.docker.local and fill in your values.
# NEVER commit .env.docker.local to version control.
#
# Usage: docker compose --env-file .env.docker.local up -d

# ─── PostgreSQL ────────────────────────────────────────────────────────
POSTGRES_USER=litellm
POSTGRES_PASSWORD=litellm_pass
POSTGRES_DB=litellm
DATABASE_URL=postgresql://litellm:litellm_pass@postgres:5432/litellm

# ─── LiteLLM ──────────────────────────────────────────────────────────
LITELLM_MASTER_KEY=sk-litellm-changeme
LITELLM_SALT_KEY=sk-salt-changeme

# ─── AWS (used by LiteLLM for Bedrock access) ─────────────────────────
# AWS credentials are resolved at runtime by start-services.sh from the
# host's SSO session (aws configure export-credentials). They are injected
# as AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_SESSION_TOKEN env vars.
# No AWS_PROFILE is needed — Docker containers use explicit credentials.
