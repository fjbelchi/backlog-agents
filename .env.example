# Backlog Toolkit - Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit the .env file to version control.

# ─── Provider API Keys ───────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
# GOOGLE_API_KEY=...

# ─── LiteLLM Proxy ──────────────────────────────────────────────────
LITELLM_MASTER_KEY=sk-litellm-...
LITELLM_BASE_URL=http://localhost:8000

# ─── Budget Controls ────────────────────────────────────────────────
BACKLOG_BUDGET_USD=1000
BACKLOG_BUDGET_WARN=0.70
BACKLOG_BUDGET_HARD_STOP=1.00

# ─── Redis (for response/semantic cache) ─────────────────────────────
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# ─── Qdrant (for semantic cache / RAG) ──────────────────────────────
# QDRANT_URL=http://localhost:6333
# QDRANT_API_KEY=

# ─── AWS (for Bedrock via LiteLLM) ──────────────────────────────────
# Set this to your AWS profile name. Used by start-services.sh to validate SSO.
# Default: "default". Profiles live in ~/.aws/config.
AWS_PROFILE=default

# ─── Docker Infrastructure ────────────────────────────────────────────
# For Docker mode, copy .env.docker to .env.docker.local and fill in values.
# See .env.docker for the full template.
# Docker mode is auto-detected when docker-compose.yml exists and Docker is running.
# Force native mode: BACKLOG_NATIVE_MODE=true
