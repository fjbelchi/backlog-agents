---
description: Initialize backlog system in current project. Creates directory structure, templates, config, and conventions. Auto-detects stack.
---

Use the backlog-toolkit:backlog-init skill to initialize a backlog system in this project.

If the user provided arguments, pass them as context: "$ARGUMENTS"

Follow the skill instructions completely — detect project stack, ask user questions, create directories, templates, and config.
