---
description: Create a high-quality backlog ticket with 6-check validation and cost estimation. Detects gaps, verifies dependencies, validates contracts.
---

Use the backlog-toolkit:backlog-ticket skill to create a new ticket.

The user's request: "$ARGUMENTS"

Follow the skill instructions completely — analyze codebase, generate ticket with all required sections, run 6 validation checks, estimate cost, and write to backlog.
